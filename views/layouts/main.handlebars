<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wrong Opinion Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/nav.js" defer></script>
</head>

<body class="bg-image">
  <header class="primary-header flex">
    <div>
      <img src="" alt="brand logo" class="nav-logo">
    </div>
  
    <button class="mobile-nav-toggle" aria-controls="primary-nav" aria-expanded="false">
      <span class="sr-only">Menu</span>
    </button>
  
    <nav>
      <ul id="primary-nav" data-visible="false" class="primary-nav flex">
        <li>
          <a href="/" class="text-black text-size-1p5">Home</a>
        </li>
        <li>
          {{#if logged_in}}
          <button class="text-black text-size-1p5" id="logout">Logout</button>
          {{else}}
          <a href="/login" class="text-black text-size-1p5">Login</a>
          {{/if}}
        </li>
        <li>
          <a href="/dashboard" class="text-black text-size-1p5">Dashboard</a>
        </li>
      </ul>
    </nav>
  </header>

  <main class="container">{{{body}}}</main>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>